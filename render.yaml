services:
  - type: web
    name: oikonomia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: "10000"